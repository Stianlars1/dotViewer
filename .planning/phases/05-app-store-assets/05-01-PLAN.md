---
phase: 05-app-store-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/assets/screenshots/]
autonomous: false
---

<objective>
Create 5 marketing screenshots that tell a compelling story about dotViewer's value.

Purpose: Screenshots are your App Store listing's first impression. They must instantly communicate "beautiful code previews in Finder" and make developers want to download.
Output: 5 polished screenshots ready for App Store Connect upload.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**App capabilities to showcase:**
- Syntax highlighting for 100+ file types
- 11 themes (including Tokyo Night, Solarized, GitHub Dark)
- Sensitive file warning banner (.env files)
- Custom file type registration
- Line numbers, file info header
</context>

<screenshot_strategy>
**The Story Arc:**

1. **HERO** → "Look how beautiful your code looks" (immediate value)
2. **SECURITY** → "We protect your sensitive files" (trust-building)
3. **THEMES** → "Make it yours" (customization)
4. **FILE TYPES** → "Works with everything" (breadth)
5. **MARKDOWN** → "More than just code" (versatility)

**Visual Principles:**
- Dark themes photograph better (more contrast, more "developer" feel)
- Show REAL code, not toy examples
- Keep Finder visible to contextualize QuickLook
- Use high-contrast content that's readable at thumbnail size
</screenshot_strategy>

<tasks>

<task type="auto">
  <name>Task 1: Create sample files for screenshots</name>
  <files>
    .planning/assets/screenshots/samples/AppViewModel.swift
    .planning/assets/screenshots/samples/.env.example
    .planning/assets/screenshots/samples/README.md
  </files>
  <action>
Create sample files that will look great in screenshots.

**File 1: AppViewModel.swift** (for hero shot)
Create a realistic Swift file that showcases syntax highlighting variety:
- Import statements (blue keywords)
- Class/struct with properties
- A function with async/await
- String interpolation
- Comments
- Type annotations
- SwiftUI property wrappers (@State, @Published)

Target: ~30-40 lines that fit nicely in a QuickLook window without scrolling.
The code should be realistic (could be from a real app), not a toy "Hello World".

```swift
import SwiftUI
import Combine

/// ViewModel for the main application state
@MainActor
final class AppViewModel: ObservableObject {
    @Published var isLoading = false
    @Published var errorMessage: String?
    @Published private(set) var items: [Item] = []

    private let apiClient: APIClient
    private var cancellables = Set<AnyCancellable>()

    init(apiClient: APIClient = .shared) {
        self.apiClient = apiClient
    }

    func loadItems() async {
        isLoading = true
        defer { isLoading = false }

        do {
            items = try await apiClient.fetchItems()
            errorMessage = nil
        } catch {
            errorMessage = "Failed to load: \(error.localizedDescription)"
        }
    }

    func deleteItem(at index: Int) {
        guard items.indices.contains(index) else { return }
        items.remove(at: index)
    }
}
```

**File 2: .env.example** (for security screenshot)
Create a realistic environment file with fake but believable credentials:

```
# Database Configuration
DATABASE_URL=postgres://user:p4ssw0rd@localhost:5432/myapp
DATABASE_POOL_SIZE=10

# API Keys (DO NOT COMMIT REAL VALUES)
STRIPE_SECRET_KEY=sk_live_51ABC...XYZ
STRIPE_WEBHOOK_SECRET=whsec_1234567890abcdef

# Authentication
JWT_SECRET=your-256-bit-secret-key-here
SESSION_EXPIRY=86400

# Third-party Services
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxx
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
```

**File 3: README.md** (for markdown screenshot)
Create a README that demonstrates markdown rendering capabilities:

```markdown
# Project Name

A brief description of what this project does.

## Features

- **Fast** - Optimized for performance
- **Beautiful** - Modern, clean interface
- **Extensible** - Plugin architecture

## Installation

\`\`\`bash
npm install project-name
\`\`\`

## Usage

\`\`\`javascript
import { Project } from 'project-name';

const app = new Project();
app.start();
\`\`\`

## License

MIT
```
  </action>
  <verify>ls -la .planning/assets/screenshots/samples/ shows all 3 files</verify>
  <done>Sample files created with realistic, visually appealing content</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Capture Screenshot 1: HERO SHOT</action>
  <instructions>
**THE MOST IMPORTANT SCREENSHOT - Take your time with this one.**

This screenshot sells the app. It must instantly communicate: "Your code will look THIS beautiful."

**Setup:**
1. Copy `AppViewModel.swift` from `.planning/assets/screenshots/samples/` to your Desktop
2. Open dotViewer app → Settings → Set theme to **Tokyo Night** (dark purple aesthetic)
3. Set font size to **14pt** (good readability at thumbnail size)
4. Enable **Show Line Numbers**
5. Enable **Show File Info Header**

**In Finder:**
1. Navigate to Desktop (or wherever you put the file)
2. Set Finder to **List View** with sidebar visible
3. Select `AppViewModel.swift`
4. Press **Space** to open QuickLook

**Composition:**
```
┌─────────────────────────────────────────────────┐
│  ┌─────────────┐                                │
│  │ Finder      │  ┌────────────────────────┐   │
│  │ sidebar     │  │  QuickLook Preview     │   │
│  │             │  │                        │   │
│  │ • Desktop ◀ │  │  [Header: Swift badge] │   │
│  │ • Documents │  │                        │   │
│  │ • Downloads │  │  1  import SwiftUI     │   │
│  │             │  │  2  import Combine     │   │
│  │             │  │  3                     │   │
│  │             │  │  4  /// ViewModel...   │   │
│  │             │  │  5  @MainActor         │   │
│  │             │  │  ...                   │   │
│  │             │  │                        │   │
│  └─────────────┘  └────────────────────────┘   │
│                                                 │
│  [Selected file: AppViewModel.swift]            │
└─────────────────────────────────────────────────┘
```

**Key elements that MUST be visible:**
- The QuickLook window should be the star (largest element)
- Line numbers visible on left
- Header showing "Swift" language badge, line count, file size
- Multiple syntax colors visible (keywords, strings, comments, types)
- Some of the Finder file list peeking out to show context

**Capture:**
- Use **Cmd+Shift+4** then **Space** to capture the QuickLook window
- OR capture the entire Finder window including QuickLook (recommended)
- Aim for the file list + QuickLook panel composition

**Resize:**
```bash
# After capturing, resize to App Store dimensions:
sips -z 1800 2880 ~/Desktop/screenshot.png --out .planning/assets/screenshots/01-hero-syntax.png
```

**Self-check before saving:**
- [ ] Can you read the code at a glance? (If not, increase font size)
- [ ] Are multiple syntax colors visible? (purple keywords, green strings, gray comments)
- [ ] Is the "Swift" language badge visible in header?
- [ ] Does it look professional and polished?
  </instructions>
  <verification>ls .planning/assets/screenshots/01-hero-syntax.png exists</verification>
  <resume-signal>Type "done" when screenshot 1 is saved</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Capture Screenshot 2: SECURITY WARNING</action>
  <instructions>
**This screenshot builds trust: "We care about your security."**

The orange warning banner for sensitive files is a key differentiator.

**Setup:**
1. Copy `.env.example` from `.planning/assets/screenshots/samples/` to Desktop
2. Rename it to just `.env` (so it triggers the security banner)
3. Keep dotViewer settings the same (Tokyo Night theme)

**In Finder:**
1. Navigate to Desktop
2. You may need to show hidden files: **Cmd+Shift+.** (period)
3. Select `.env`
4. Press **Space** to open QuickLook

**Composition:**
```
┌─────────────────────────────────────────────────┐
│  ┌────────────────────────────────────────────┐ │
│  │  QuickLook Preview                         │ │
│  │                                            │ │
│  │  [Header: env badge, lines, size]          │ │
│  │                                            │ │
│  │  ┌──────────────────────────────────────┐  │ │
│  │  │ 🔒 This file may contain sensitive   │  │ │
│  │  │    data (API keys, passwords)        │  │ │
│  │  └──────────────────────────────────────┘  │ │
│  │                                            │ │
│  │  1  # Database Configuration              │ │
│  │  2  DATABASE_URL=postgres://user:...      │ │
│  │  3  DATABASE_POOL_SIZE=10                 │ │
│  │  4                                        │ │
│  │  5  # API Keys                            │ │
│  │  6  STRIPE_SECRET_KEY=sk_live_...         │ │
│  │  ...                                      │ │
│  └────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**Key elements that MUST be visible:**
- The **orange security banner** is the star of this screenshot
- Shield icon (🔒) clearly visible
- Warning text readable: "This file may contain sensitive data"
- Some of the .env content visible below (with realistic-looking API keys)
- The contrast between the orange warning and the dark theme

**Capture:**
- Capture just the QuickLook window (Cmd+Shift+4 → Space → click window)
- The banner should be prominently visible near the top

**Resize:**
```bash
sips -z 1800 2880 ~/Desktop/screenshot.png --out .planning/assets/screenshots/02-security-warning.png
```

**Self-check before saving:**
- [ ] Is the orange warning banner clearly visible and readable?
- [ ] Can you see the shield icon?
- [ ] Is the .env content realistic-looking?
- [ ] Does it communicate "security-conscious app"?
  </instructions>
  <verification>ls .planning/assets/screenshots/02-security-warning.png exists</verification>
  <resume-signal>Type "done" when screenshot 2 is saved</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Capture Screenshot 3: THEME CUSTOMIZATION</action>
  <instructions>
**This screenshot shows customization: "Make it yours."**

Show the Settings view with the theme picker and preview.

**Setup:**
1. Open dotViewer app
2. Click **Settings** in sidebar
3. Scroll so you can see:
   - Theme picker dropdown
   - Font size slider
   - Show Line Numbers toggle
   - Theme Preview section at bottom

**Before capturing, cycle through a few themes to land on a good one:**
- Try **GitHub Dark** or **Solarized Dark** for variety from the hero shot
- Make sure the Theme Preview shows the example code with colors

**Composition:**
```
┌─────────────────────────────────────────────────┐
│  dotViewer                                      │
│  ┌──────────┐  ┌──────────────────────────────┐│
│  │ Sidebar  │  │        Settings              ││
│  │          │  │                              ││
│  │ Status   │  │  Appearance                  ││
│  │ File Ty..│  │  ┌────────────────────────┐  ││
│  │ Settings◀│  │  │ Theme: [Solarized Dark▼]│  ││
│  │          │  │  │                        │  ││
│  │          │  │  │ Font Size      14pt    │  ││
│  │          │  │  │ ════════●════════════  │  ││
│  │          │  │  │                        │  ││
│  │          │  │  │ ☑ Show Line Numbers   │  ││
│  │          │  │  └────────────────────────┘  ││
│  │          │  │                              ││
│  │          │  │  Theme Preview               ││
│  │          │  │  ┌────────────────────────┐  ││
│  │          │  │  │ // Example code preview│  ││
│  │          │  │  │ func greet(name:...    │  ││
│  │          │  │  └────────────────────────┘  ││
│  └──────────┘  └──────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**Key elements that MUST be visible:**
- The theme picker dropdown (showing selected theme name)
- Font size slider
- Line numbers toggle
- The Theme Preview section showing colored code
- The sidebar showing "Settings" is selected

**Capture:**
- Capture the entire dotViewer window: **Cmd+Shift+4** → **Space** → click the window
- Or use Screenshot.app for more control

**Resize:**
```bash
sips -z 1800 2880 ~/Desktop/screenshot.png --out .planning/assets/screenshots/03-theme-settings.png
```

**Self-check before saving:**
- [ ] Is the theme dropdown clearly showing a theme name?
- [ ] Is the Theme Preview visible with colored code?
- [ ] Does the app look polished and professional?
- [ ] Can someone understand "this app has theme options" at a glance?
  </instructions>
  <verification>ls .planning/assets/screenshots/03-theme-settings.png exists</verification>
  <resume-signal>Type "done" when screenshot 3 is saved</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Capture Screenshot 4: FILE TYPE SUPPORT</action>
  <instructions>
**This screenshot shows breadth: "Works with everything."**

Show the File Types view with its categorized list.

**Setup:**
1. Open dotViewer app
2. Click **File Types** in sidebar
3. Expand a few categories to show variety:
   - Programming Languages (Swift, Python, JavaScript, etc.)
   - Config Files (JSON, YAML, etc.)
   - Dotfiles section
4. Make sure the search bar is empty (showing all types)

**Composition:**
```
┌─────────────────────────────────────────────────┐
│  dotViewer                                      │
│  ┌──────────┐  ┌──────────────────────────────┐│
│  │ Sidebar  │  │       File Types             ││
│  │          │  │  ┌─────────────────────────┐ ││
│  │ Status   │  │  │ 🔍 Search file types... │ ││
│  │ File Ty..◀│  │  └─────────────────────────┘ ││
│  │ Settings │  │                              ││
│  │          │  │  ▼ Programming Languages     ││
│  │          │  │    ☑ Swift        .swift     ││
│  │          │  │    ☑ Python       .py        ││
│  │          │  │    ☑ JavaScript   .js, .mjs  ││
│  │          │  │    ☑ TypeScript   .ts, .tsx  ││
│  │          │  │    ☑ Rust         .rs        ││
│  │          │  │    ☑ Go           .go        ││
│  │          │  │                              ││
│  │          │  │  ▼ Config Files              ││
│  │          │  │    ☑ JSON         .json      ││
│  │          │  │    ☑ YAML         .yaml, .yml││
│  │          │  │    ☑ TOML         .toml      ││
│  └──────────┘  └──────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**Key elements that MUST be visible:**
- Multiple categories expanded (Programming Languages, Config Files, etc.)
- File type toggles (the switches)
- File extension badges (.swift, .py, .js, etc.)
- The "Add Custom" button in top right
- Enough variety to communicate "100+ file types"

**Pro tip:** Scroll the list so you show the most recognizable languages near the top (Swift, Python, JavaScript, TypeScript, Rust, Go).

**Capture:**
- Capture the entire dotViewer window

**Resize:**
```bash
sips -z 1800 2880 ~/Desktop/screenshot.png --out .planning/assets/screenshots/04-file-types.png
```

**Self-check before saving:**
- [ ] Are multiple categories visible?
- [ ] Can you see at least 6-8 different file types?
- [ ] Are the extension badges (.swift, .py, etc.) readable?
- [ ] Does it communicate "supports lots of file types"?
  </instructions>
  <verification>ls .planning/assets/screenshots/04-file-types.png exists</verification>
  <resume-signal>Type "done" when screenshot 4 is saved</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Capture Screenshot 5: MARKDOWN RENDERING</action>
  <instructions>
**This screenshot shows versatility: "More than just code."**

Show the markdown rendering capability with the rendered/raw toggle.

**Setup:**
1. Copy `README.md` from `.planning/assets/screenshots/samples/` to Desktop
2. In dotViewer Settings, set **Markdown Preview** to **Rendered** (the toggle)
3. Keep the same theme (or switch to a light theme like GitHub Light for contrast)

**In Finder:**
1. Navigate to Desktop
2. Select `README.md`
3. Press **Space** to open QuickLook

**Composition:**
```
┌─────────────────────────────────────────────────┐
│  ┌────────────────────────────────────────────┐ │
│  │  QuickLook Preview                         │ │
│  │                                            │ │
│  │  [Header: md badge] [Rendered/Raw toggle]  │ │
│  │                                            │ │
│  │  # Project Name                            │ │
│  │                                            │ │
│  │  A brief description of what this...       │ │
│  │                                            │ │
│  │  ## Features                               │ │
│  │                                            │ │
│  │  • **Fast** - Optimized for performance   │ │
│  │  • **Beautiful** - Modern, clean interface│ │
│  │  • **Extensible** - Plugin architecture   │ │
│  │                                            │ │
│  │  ## Installation                           │ │
│  │                                            │ │
│  │  ┌────────────────────────────────────┐   │ │
│  │  │ npm install project-name           │   │ │
│  │  └────────────────────────────────────┘   │ │
│  │                                            │ │
│  └────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**Key elements that MUST be visible:**
- The **Rendered/Raw toggle** in the header (shows the purple toggle button)
- Formatted markdown: headers, bullet points, **bold** text
- Code block with syntax highlighting
- Clean, Typora-inspired rendering

**Alternative:** If you prefer, show the RAW view with syntax-highlighted markdown instead. The toggle should still be visible.

**Capture:**
- Capture just the QuickLook window

**Resize:**
```bash
sips -z 1800 2880 ~/Desktop/screenshot.png --out .planning/assets/screenshots/05-markdown-rendered.png
```

**Self-check before saving:**
- [ ] Is the Rendered/Raw toggle visible?
- [ ] Does the markdown look nicely formatted?
- [ ] Is the code block visible with a different background?
- [ ] Does it communicate "beautiful markdown previews"?
  </instructions>
  <verification>ls .planning/assets/screenshots/05-markdown-rendered.png exists</verification>
  <resume-signal>Type "done" when screenshot 5 is saved</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Verify all screenshots meet requirements</name>
  <files>N/A (verification only)</files>
  <action>
Verify all 5 screenshots exist and meet App Store requirements:

1. Check all files exist:
```bash
ls -la .planning/assets/screenshots/*.png
```

2. Check dimensions of each (must be 2880x1800 or similar valid size):
```bash
for f in .planning/assets/screenshots/*.png; do
  echo "=== $f ==="
  sips -g pixelHeight -g pixelWidth "$f"
done
```

Valid App Store dimensions for Mac screenshots:
- 2880 × 1800 (16:10, Retina)
- 2560 × 1600 (16:10)
- 1440 × 900 (16:10)
- 1280 × 800 (16:10)

3. If any screenshots are wrong size, provide resize commands.

4. List final inventory:
- 01-hero-syntax.png - Hero shot with Swift code
- 02-security-warning.png - .env file with warning banner
- 03-theme-settings.png - Settings view with theme picker
- 04-file-types.png - File Types view
- 05-markdown-rendered.png - Markdown rendering
  </action>
  <verify>All 5 screenshots exist with valid dimensions</verify>
  <done>All screenshots verified as App Store compliant</done>
</task>

<task type="auto">
  <name>Task 8: Update requirements doc with final inventory</name>
  <files>.planning/assets/screenshots/README.md</files>
  <action>
Update the README.md with the final screenshot inventory and descriptions for App Store Connect:

Include for each screenshot:
- Filename
- Suggested App Store caption (short, compelling)
- What it showcases

Example captions:
1. "Beautiful syntax highlighting for 100+ file types"
2. "Security warnings for sensitive files"
3. "11 themes to match your style"
4. "Support for every language you use"
5. "Gorgeous markdown rendering"
  </action>
  <verify>cat .planning/assets/screenshots/README.md shows complete inventory</verify>
  <done>README updated with screenshot descriptions and App Store captions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 screenshots captured and saved
- [ ] All screenshots are valid App Store dimensions (2880x1800 recommended)
- [ ] Screenshots tell a coherent story (hero → security → customization → breadth → versatility)
- [ ] README.md updated with captions for App Store Connect
</verification>

<success_criteria>
- 5 marketing screenshots ready for App Store Connect
- Each screenshot has a clear purpose and visual focus
- Screenshots showcase dotViewer's key value propositions
- All images meet Apple's technical requirements
</success_criteria>

<output>
After completion, create `.planning/phases/05-app-store-assets/05-01-SUMMARY.md`
</output>
