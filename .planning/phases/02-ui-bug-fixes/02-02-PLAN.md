---
phase: 02-ui-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [dotViewer/ContentView.swift]
autonomous: true
---

<objective>
Apply destructive styling to uninstall button (Bug #3).

Purpose: The uninstall button in the "Danger Zone" section should have red/destructive styling to clearly indicate it's a destructive action. Currently it uses `.buttonStyle(.bordered)` which doesn't show red despite having `role: .destructive`.
Output: Red-styled uninstall button that visually communicates danger.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dotViewer/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update uninstall button to use destructive styling</name>
  <files>dotViewer/ContentView.swift</files>
  <action>
Find the uninstall button in SettingsView (around lines 463-469):

Current code:
```swift
Button(role: .destructive) {
    uninstallApp()
} label: {
    Label("Uninstall dotViewer", systemImage: "trash")
        .frame(maxWidth: .infinity)
}
.buttonStyle(.bordered)
```

Change `.buttonStyle(.bordered)` to use proper destructive styling:
```swift
Button(role: .destructive) {
    uninstallApp()
} label: {
    Label("Uninstall dotViewer", systemImage: "trash")
        .frame(maxWidth: .infinity)
}
.buttonStyle(.borderedProminent)
.tint(.red)
```

This makes the button:
- `.borderedProminent` - filled button style that respects role/tint
- `.tint(.red)` - explicitly red color for the destructive action
  </action>
  <verify>Build succeeds: xcodebuild -project dotViewer.xcodeproj -scheme dotViewer -quiet build</verify>
  <done>Uninstall button has red/destructive styling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds without errors
- [ ] No new warnings introduced
- [ ] Uninstall button appears red in the UI
</verification>

<success_criteria>
- Uninstall button is visually red/destructive
- Button style is prominent (filled, not just outlined)
- Build passes
- No regressions to button functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-bug-fixes/02-02-SUMMARY.md`
</output>
