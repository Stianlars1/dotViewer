# Complete GPX and GAP Quick Look support

Authorized by the user after independent review of issues #24/#25 and PR #26.

- GPX reuses XML. Support both dotViewer's export and com.topografix.gpx in each extension and the generator. Validate generated bundle routing, not just registry keys.
- Ship GAP .g/.gi and GAP test .tst routing, two pinned MIT tree-sitter grammars and queries. GAP test input regions must be parsed with GAP, preserving original byte offsets and multiline input; expected output remains plain text.
- Preserve Godot .gd defaults. Conservative GAP content detection plus explicit custom overrides, retaining Godot for ambiguous input. Share resolution between custom panel, Quick Look and thumbnails.
- Verify actual highlighted tokens/HTML, cancellation and Unicode preservation; then installed Finder Space and Option+Space on GPX/GAP/Godot controls.
- Update PR #26 on its existing branch, rewrite description for final scope, document results, and leave a distinct tested development build installed. No merge or public release requested.
